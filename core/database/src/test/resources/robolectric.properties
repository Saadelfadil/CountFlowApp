# Pinned rather than following compileSdk. Room's behaviour here is SQLite behaviour, which does
# not vary by platform level, and pinning keeps the suite from breaking every time compileSdk
# moves ahead of the newest android-all jar Robolectric has published.
sdk=34
